spring:
  datasource:
    url: jdbc:mysql://database-1.csmh5vzvstef.ap-northeast-2.rds.amazonaws.com:3306/mileage?autoReconnect=true #TODO:낼 때는 로컬 디비로 바꿔서 낼것 jdbc:mysql://localhost:3306/mileage?serverTimezone=Asia/Seoul
    username: admin
    password: capstonedb
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: none


logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
